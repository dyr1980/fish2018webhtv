<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <androidx.media3.ui.DefaultTimeBar
        android:id="@+id/timeBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:nextFocusLeft="@id/timeBar"
        android:nextFocusRight="@id/timeBar"
        android:nextFocusUp="@id/next"
        android:nextFocusDown="@id/timeBar"
        app:buffered_color="@color/md_paper_70"
        app:played_color="@color/md_amber"
        app:scrubber_color="@color/md_amber"
        app:unplayed_color="@color/md_line_2" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/position"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFeatureSettings="tnum"
            android:textColor="@color/md_paper"
            android:textSize="15sp"
            android:textStyle="bold"
            tools:text="00:00:00" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFeatureSettings="tnum"
            android:textColor="@color/md_paper_70"
            android:textSize="15sp"
            tools:text="00:00:00" />

    </LinearLayout>

</androidx.appcompat.widget.LinearLayoutCompat>
