diff --git a/DOCS/man/options.rst b/DOCS/man/options.rst
--- a/DOCS/man/options.rst
+++ b/DOCS/man/options.rst
@@ -7181,23 +7181,4 @@ them.
 
     Android with ``--gpu-context=android`` only.
 
-``--android-vulkan-aimagereader-backend=<auto|direct|compute|fragment>``
-    Select how Android MediaCodec ``AHardwareBuffer`` frames are imported for
-    Vulkan rendering:
-
-    :auto: Prefer direct YCbCr sampling. If direct sampling is unavailable,
-           select compute or fragment conversion while preferring the
-           narrowest suitable output format. Compute wins when both backends
-           support the same format. This is the default.
-    :direct: Require direct YCbCr sampling without conversion fallback.
-    :compute: Require compute conversion without direct or fragment fallback.
-    :fragment: Require fragment conversion without direct or compute fallback.
-
-    The forced modes are intended for device compatibility, power, and
-    performance comparisons. If the selected backend is unsupported or fails,
-    mpv does not silently switch to another AImageReader backend. Normal
-    hardware-decoding fallback rules still apply.
-
-    Android Vulkan while using the MediaCodec hardware decoder only.
-
 ``--android-dolby-vision-output=<configured|direct>``
diff --git a/meson.build b/meson.build
--- a/meson.build
+++ b/meson.build
@@ -1415,9 +1415,7 @@ if features['android-media-ndk']
     sources += files('video/out/hwdec/hwdec_aimagereader.c')
 endif
 if features['android-media-ndk'] and features['vulkan']
-    sources += files('video/out/hwdec/hwdec_aimagereader_vk.c',
-                     'video/out/hwdec/hwdec_aimagereader_vk_convert.c',
-                     'video/out/hwdec/hwdec_aimagereader_vk_direct.c')
+    sources += files('video/out/hwdec/hwdec_aimagereader_vk.c')
 endif
 
 d3d_hwaccel = get_option('d3d-hwaccel').require(
diff --git a/options/options.c b/options/options.c
--- a/options/options.c
+++ b/options/options.c
@@ -255,15 +255,6 @@ static const m_option_t mp_vo_opt_list[] = {
     {"android-dolby-vision-output", OPT_CHOICE(android_dolby_vision_output,
         {"configured", ANDROID_DOLBY_VISION_OUTPUT_CONFIGURED},
         {"direct", ANDROID_DOLBY_VISION_OUTPUT_DIRECT}), .flags = UPDATE_VO},
-    {"android-vulkan-aimagereader-backend",
-        OPT_CHOICE(android_vulkan_aimagereader_backend,
-            {"auto", ANDROID_VULKAN_AIMAGEREADER_BACKEND_AUTO},
-            {"direct", ANDROID_VULKAN_AIMAGEREADER_BACKEND_DIRECT},
-            {"compute", ANDROID_VULKAN_AIMAGEREADER_BACKEND_COMPUTE},
-            {"fragment", ANDROID_VULKAN_AIMAGEREADER_BACKEND_FRAGMENT}),
-        .flags = UPDATE_VO},
-    {"android-vulkan-conversion-backend",
-        OPT_REPLACED("android-vulkan-aimagereader-backend")},
     {"android-osd-wid", OPT_INT64(android_osd_wid),
         .flags = UPDATE_VO},
     {"android-osd-surface-size", OPT_SIZE_BOX(android_osd_surface_size)},
diff --git a/options/options.h b/options/options.h
--- a/options/options.h
+++ b/options/options.h
@@ -11,11 +11,4 @@ enum android_dolby_vision_output {
     ANDROID_DOLBY_VISION_OUTPUT_DIRECT,
 };
 
-enum android_vulkan_aimagereader_backend {
-    ANDROID_VULKAN_AIMAGEREADER_BACKEND_AUTO = 0,
-    ANDROID_VULKAN_AIMAGEREADER_BACKEND_DIRECT,
-    ANDROID_VULKAN_AIMAGEREADER_BACKEND_COMPUTE,
-    ANDROID_VULKAN_AIMAGEREADER_BACKEND_FRAGMENT,
-};
-
 typedef struct mp_vo_opts {
@@ -99,6 +92,5 @@ typedef struct mp_vo_opts {
 
     struct m_geometry android_surface_size;
     int android_dolby_vision_output;
-    int android_vulkan_aimagereader_backend;
     int64_t android_osd_wid;
     struct m_geometry android_osd_surface_size;
diff --git a/video/out/hwdec/hwdec_aimagereader.c b/video/out/hwdec/hwdec_aimagereader.c
--- a/video/out/hwdec/hwdec_aimagereader.c
+++ b/video/out/hwdec/hwdec_aimagereader.c
@@ -66,5 +66,4 @@ struct priv_owner {
 #if HAVE_VULKAN
     mp_mutex vk_lock;
     struct aimagereader_vk *vk;
-    enum android_vulkan_aimagereader_backend vk_backend;
 #endif
@@ -406,7 +405,5 @@ static int init(struct ra_hwdec *hw)
 
 #if HAVE_VULKAN
     mp_mutex_init(&p->vk_lock);
-    p->vk_backend = hw->ra_ctx->vo->opts->android_vulkan_aimagereader_backend;
-    use_vulkan = aimagereader_vk_available(hw->ra_ctx, hw->log,
-                                           p->vk_backend);
+    use_vulkan = aimagereader_vk_available(hw->ra_ctx, hw->log);
 #endif
@@ -593,9 +590,8 @@ static int mapper_init(struct ra_hwdec_mapper *mapper)
         const struct aimagereader_vk_api api = {
             .AImage_delete = o->AImage_delete,
-            .AImage_deleteAsync = o->AImage_deleteAsync,
             .AHardwareBuffer_describe = o->AHardwareBuffer_describe,
         };
         p->backend = MAPPER_BACKEND_VULKAN;
-        p->vk = aimagereader_vk_create(mapper, &api, o->vk_backend);
+        p->vk = aimagereader_vk_create(mapper, &api);
         if (!p->vk)
             return -1;
diff --git a/video/out/vulkan/context.c b/video/out/vulkan/context.c
--- a/video/out/vulkan/context.c
+++ b/video/out/vulkan/context.c
@@ -217,4 +217,5 @@ pl_vulkan mppl_create_vulkan(struct vulkan_opts *opts,
 #if HAVE_ANDROID_MEDIA_NDK
         "VK_ANDROID_external_memory_android_hardware_buffer",
         "VK_EXT_queue_family_foreign",
+        "VK_KHR_external_semaphore_fd",
 #endif
